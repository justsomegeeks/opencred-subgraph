specVersion: 0.0.2
schema:
  file: schema.graphql
dataSources:
  - kind: ethereum/contract
    name: OpenCredFactory
    network: mainnet
    source:
      address: "0x3d2EFecB6b794d72eb3341A30448a766364930F7"
      abi: OpenCredFactory
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - BootcampCreated
        - OwnershipTransferred
      abis:
        - name: OpenCredFactory
          file: OpenCredFactory/abis/OpenCredFactory.json
      eventHandlers:
        - event: BootcampCreated(indexed address,indexed address,string)
          handler: handleBootcampCreated
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleOwnershipTransferred
      file: OpenCredFactory/OpenCredFactory.wasm
